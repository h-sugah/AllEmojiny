# To get started with Dependabot version updates, you'll need to specify which
# package ecosystems to update and where the package manifests are located.
# Please see the documentation for all configuration options:
# https://docs.github.com/code-security/dependabot/dependabot-version-updates/configuration-options-for-the-dependabot.yml-file

version: 2
updates:
  - package-ecosystem: "npm" # See documentation for possible values
    directory: "/" # Location of package manifests
    schedule:
      interval: "weekly"
    groups:
      # vite and its plugin have interlocking peer-dependency ranges;
      # bumping one without the other breaks npm install (ERESOLVE).
      vite:
        patterns:
          - "vite"
          - "@vitejs/*"
      # tailwindcss v3->v4 moved the PostCSS plugin into a separate
      # package and changed the CSS entry syntax; keep them in lockstep.
      tailwind:
        patterns:
          - "tailwindcss"
          - "@tailwindcss/*"
